<% if @sb[:active_tab] == "saved_reports" %>
  <% if @view && !@view.table.data.empty? %>
    <%= render :partial => 'layouts/x_gtl'%>
  <% else %>
     <div class="bg-warning">
      <span class="glyphicon glyphicon-info-sign"></span>
      <strong>No Saved Reports available.</strong>
    </div>
  <% end %>
<% end %>
