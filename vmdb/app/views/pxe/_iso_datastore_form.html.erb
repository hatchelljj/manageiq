<% url = url_for(:action => 'iso_datastore_form_field_changed', 
                 :id     => "#{@isd.id || "new"}") 
%>
<div id="form_div">
	<%= render :partial => "layouts/flash_msg" %>
  <p class="lead">Basic Information</p>
  <table class="table table-striped">
    <tbody>
      <tr>
          <td ><%= ui_lookup(:table => "ext_management_system")%></td>
          <td >
            <%= select_tag("ems_id",
                           options_for_select([["<Choose>",nil]] + @edit[:emses].sort,
                           @edit[:new][:ems_id]),
                           "data-miq_observe" => {:url => url}.to_json) 
            %>
          </td>
        </tr>
    </tbody>
  </table>
</div>
