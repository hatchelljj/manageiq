<div id="template_folders_div" style="width:100%;height:100%;overflow:auto;">
	<% if @folders %>
		<%= render(:partial=>"layouts/flash_msg") %>
      <table class="table table-striped">
        <thead>
          <th></th><th></th>
        </thead>
        <tbody>
        <tr onclick="cfmeDynatree_activateNode('customization_templates_tree', 'xx-xx-system');"
            onmouseover="this.style.cursor='pointer'" title="View 'Examples (read only)' Folder">
          <td  nowrap>
            <img src="/images/icons/new/folder.png" valign="middle" border="0" height="20" width="20" />
          </td>
          <td>
            Examples (read only)
          </td>
        </tr>
        <% @folders.each do |f| %>
          <tr onclick="cfmeDynatree_activateNode('customization_templates_tree', 'xx-xx-<%= to_cid(f.id) %>');"
            onmouseover="this.style.cursor='pointer'" title="View '<%= f.name %>' Folder">
            <td  nowrap>

                <img src="/images/icons/new/folder.png" valign="middle" border="0" height="20" width="20" />

            </td>
            <td>
              <%= h(f.name) %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
	<% end %>
</div>

