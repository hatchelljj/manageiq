<div id="alert_list_div" style="width:100%;height:100%;overflow:auto;">
  <% if x_active_tree == :alert_tree && @view %>
    <% if @view.table.data.empty? %>
      <div class="bg-warning">
        <i class="fa fa-info-circle"></i>
        <strong>No Alerts are defined<%= @search_text.blank? ? "" : " that match the entered search string" %>.</strong>
      </div>
    <% else %>
      <div id='main_div'>
        <%= render :partial => 'layouts/gtl', 
                   :locals  => {:action_url => "alert_get_all", 
                                :button_div => 'policy_bar'} 
        %>
      </div>
    <% end %>
  <% end %>
</div>
