- if items
  %table.table.table-striped
    %thead
      %tr
        %th{:colspan => 6, :align => "left"}
          =title
    - labels.each do |label|
      %td = label
    - items.each do |item|
      %tr
        - item.each do |value|
          %td= value
